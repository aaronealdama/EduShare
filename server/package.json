{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "start-development": "nodemon server.js --exec babel-node",
    "start:production": "node build/server.js",
    "build": "babel server.js -d build -s",
    "seed" : "cd seed && node seed.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mongoose": "^5.5.15",
    "morgan": "^1.10.0",
    "per-env": "^1.0.2"
  }
}
